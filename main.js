
let raceNumber = Math.floor(Math.random() * 1000);

var raceTime = false;

const runnersAge = 15;

if(runnersAge >= 18 && raceTime === true) {
    raceNumber = raceNumber + 1000};

if(runnersAge >= 18 && raceTime === true) {
    console.log(`Your race will begin at 9:30 am runner ${raceNumber}. `);
}else if(runnersAge >= 18 && raceTime === false) {
    console.log(`Your race will begin at 11:00 am runner ${raceNumber}.`)};

if(runnersAge < 18) console.log(`Your race will begin at 12:30 pm ${raceNumber}.`);
